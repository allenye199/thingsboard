====================================================================
#单元按钮自定义动作函数实现



在 entities table widget上：

在widget每一行中增加了 删除、编辑、二个行单元按钮， 。
表头增加 "增加"按钮；
在为每个按钮配置动作时，动作类型选择“自定义动作”；
为自定义动作函数实现代码: f($event, widgetContext, entityId, entityName, additionalParams, entityLabel)
函数功能如下：
增加：弹出customer对话框（数据为空），确认后在最后一行增加一行客户数据。刷新界面。 
删除：弹出确认对话框，确认后删除当前行客户数据。刷新界面。
编辑：弹出customer对话框（数据为当前行客户数据）。确认后刷新界面。 


参见 /docs/widget/ 的文档实现